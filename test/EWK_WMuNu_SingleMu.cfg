# CSA07 Skim for W->MuNu Events
# Filter events with single high-pt tracks
# then produces AODSIM selected events
# Created by Ezio Torassa
# Tested on 2007/06/26/

process WmunuSkim= {

    service = AdaptorConfig {}
    
    source = PoolSource {
	untracked vstring fileNames =
	{
        "file:/raid3/data/MCdata/1_3_1/Wmunu/Wmunu_001.root"
	}
	untracked int32 maxEvents = -1
	untracked uint32 debugVerbosity = 0
	untracked bool   debugFlag     = false
    }

    ### keep the logging output to a nice level ###
    include "FWCore/MessageLogger/data/MessageLogger.cfi"
    include "SimGeneral/HepPDTESSource/data/pythiapdt.cfi"

    ## Paths and Modules
    include "ElectroWeakAnalysis/WReco/data/WMuNuSkim.cff"

    include "Configuration/EventContent/data/EventContent.cff"    

    module AODSIM1 = PoolOutputModule
    {
        untracked string fileName = "WMuNuFiltered.root"
        untracked string filterName = "WMuNuFilter"
        untracked string dataTier = "AODSIM"
        using AODSIMEventContent
	untracked PSet SelectEvents = 
	{
	    vstring SelectEvents = {"WMuNuFilterPath1muIso","WMuNuFilterPath1muNoIso"}
	}
    }


   endpath outpathFull = { AODSIM1}

}
